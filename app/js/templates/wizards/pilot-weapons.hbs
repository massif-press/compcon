{{#items}}
<tr>
  <td style="padding-bottom: 5px; ">
    <div class="menu-border equip-expander">
      <div class="equip-expander-header sweep-btn" style="text-align:center;" data-item="{{id}}">
        <span><b>{{name}}</b></span>
      </div>
      <div class="equip-open-info hidden" style="background-color: var(--bg);">
        <div style="padding-left:5px">
        {{#if description}}
          <blockquote class="desc-text">{{{description}}}</blockquote>
        {{/if}}
        {{#if effect}}
          <span>{{effect}}</span>
          <br>
        {{/if}}
        <i>Rarity <b>{{rarity}}</b></i>
        <table class="gear-table">
          <tr>
            <th>Range</th>
            <th>Damage</th>
          <tr>
            <td>{{range}}</td>
            <td>{{{damage}}} Damage</td>
        </table>
        <div class="tag-div">
          {{#tags}}
            <div class="tag">
              <a href="#" data-balloon-length="{{balloonSize description}}" data-balloon="{{description}}" data-balloon-pos="right">{{name}}</a>
            </div>
          {{/tags}}
        </div>  
      </div>
    </div>
  </td>
  <td style="vertical-align: top" width="24px;">
    <div class="pilot-weapon-btn" data-item="{{id}}" data-item-name="{{name}}">
      <span class="pull-right add-button">
        <a href="#" data-balloon-length="small" data-balloon="Add Item" data-balloon-pos="left">+</a>
      </span>
      <span class="pull-right subtract-button" style="display: none;" data-item="{{id}}">
        <a href="#" data-balloon-length="medium" data-balloon="Remove Item" data-balloon-pos="left">-</a>
      </span>
    </div>
  </td>
</tr>
{{/items}}